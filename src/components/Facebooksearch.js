


const Facebooksearch = (props) => {

    return (
        <div className="col-1">
            <button type="submit" className="btn btn-secondary mt-2">{props.name}</button>
        </div>
        
    )
}

export default Facebooksearch